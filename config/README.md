# config

* `includes.binary/`: Files to be included on the medium filesystem
* `includes.chroot/`: Files to be added or replaced in the live file system. A typical use is to populate the skeleton user directory (`/etc/skel`)
  used by the live system to create the live user's home directory.

